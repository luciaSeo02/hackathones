import 'dotenv/config';

export const {
    MYSQL_HOST,
    MYSQL_USER,
    MYSQL_PASSWORD,
    MYSQL_DATABASE,
    MYSQL_PORT,
    ADMIN_PASSWORD,
    HOST,
    PORT,
    SECRET,
    SMTP_USER,
    SMTP_API_KEY,
    SMTP_HOST,
    SMTP_PASS,
    SMTP_PORT,
    UPLOAD_DIR
} = process.env;
